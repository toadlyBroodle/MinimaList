package ca.toadlybroodledev.sublist;

import androidx.fragment.app.Fragment;

/**
 * Forward-compat stub so FragmentManager can restore saved state on devices
 * that had the full ProfileFragment in their saved FM state before Phase 10.4
 * deleted it. Drop this in Phase 10.2 during the package rename.
 */
public class ProfileFragment extends Fragment {
}
